using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class MovementEffect : MonoBehaviour {

	/*public float value;
	public float duration;
	public Monster target;
	public string source;

	void Start ()
	{
		target.speed = target.speed * (1- value);
	}

	void Update(){
		duration -= Time.deltaTime;

		if (duration <= 0) {
			target.speed = target.speed / (1-value);
			target.movementEffects.Remove (gameObject);
			target.speedChangeSource.Remove (source);
			Destroy (gameObject);
		}
	}*/
}


